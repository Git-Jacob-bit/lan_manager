server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # To zapobiega błędom 404 przy korzystaniu z React Routera
        try_files $uri $uri/ /index.html;
    }
}